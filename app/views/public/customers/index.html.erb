<!--このviweは退会機能確認後削除 必要ないview-->

<div class="container">
  <div class="row">
    <div class="col">
      <h4 class="my-5">会員一覧</h4>
        <table class="table">
          <thead>
            <tr>
              <th>氏名</th>
              <th>ステータス</th>
            </tr>
          </thead>
          <% @customers.each do |customer| %>
            <tr>
              <td><%= customer.full_name %></td>
              <td><%= customer.is_active %></td>
            </tr>
          <% end %>
        </table>
    </div>
  </div>
</div>