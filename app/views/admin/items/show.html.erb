<h4>商品詳細</h4>
<%= image_tag(user.get_item_image, height: 80, width: 'auto') %>
<tr>
  <td>商品名</td>
  <td><%= item.name %></td>
</tr>
<tr>
  <td>商品説明</td>
  <td><%= item.introduction %></td>
</tr>
<tr>
  <td><%= f.label :genre_id, "ジャンル" %></td>
  <td><%= @item.genre.name %></td>
</tr>
<tr>
  <td>税込価格<p>(税抜価格)</p></td>
  <td><%= (@item.price * 1.1).round %> (<%= @item.price %>) 円</td>
</tr>
<tr>
  <td>販売ステータス</td>
  <td><%= @item.selling_status %></td>
</tr>
